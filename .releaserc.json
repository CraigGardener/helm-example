{
  "branch": "develop",
  "verifyConditions": [
    "@semantic-release/git",
    {
      "path": "@semantic-release/gitlab",
      "gitlabUrl": "https://gitlab.craiggardener.uk"
    }
  ],
  "prepare": [
    {
      "path": "@semantic-release/exec",
      "cmd": "git checkout master; git pull; git merge --no-commit origin/develop; curl https://raw.githubusercontent.com/kubernetes/helm/master/scripts/get | bash; helm plugin install https://github.com/mbenabda/helm-local-chart-version; helm local-chart-version set ${nextRelease.notes}"
    },
    {
      "path": "@semantic-release/git",
      "assets": ["Chart.yaml", "CHANGELOG.md"],
      "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }
  ],
  "publish": [
    {
      "path": "@semantic-release/gitlab",
      "gitlabUrl": "https://gitlab.craiggardener.uk"
    }
  ],
  "success": false,
  "fail": false
}
